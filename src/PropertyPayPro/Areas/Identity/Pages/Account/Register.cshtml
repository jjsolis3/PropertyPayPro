@page
@model RegisterModel
@{
    ViewData["Title"] = "Not Found";
}

<h4 class="text-center mb-4">Registration disabled</h4>
<p class="text-muted text-center">Accounts are created by an administrator.</p>
<div class="text-center mt-3">
    <a asp-area="Identity" asp-page="/Account/Login" class="btn btn-primary">Go to Login</a>
</div>
